# 0.1.17 调研报告 — dsh alpha 集成面复验 v3（第 1 轮独立重取证，r2 修订）

> 版本 1.1（r2 修订） | 日期 2026-09-02 | 状态：待评审
> 作者：架构师（A1 链第一环，第 2 轮修订） | 角色：架构师 | 结论：**无需适配（no-adaptation）** | 置信度：高

## provenance（取证基线）

| 项 | 值 |
|---|---|
| alpha 源码仓库 | `D:/working/projects/deepseek-harness`（只读分析，本轮未修改其任何文件） |
| git HEAD | `4e84901e6471b79ec0338099867ebb4606d12bb5`（分支 master） |
| 最近提交 | 2026-09-01 23:37:26 +0800，`Merge pull request #3427 from deepseek-harness/release/dsh-0.1.2-alpha.4` |
| describe | `dsh-v0.1.2-alpha.1-648-g4e84901e64` |
| 源码树版本 | 仓库根 `0.1.2-alpha.4`；`apps/cli` = `@deepseek-ai/dsh` `0.1.2-alpha.4` |
| 本地远端引用 | `origin/master` 最新即 `4e84901e64`（本轮只读约束未执行 `git fetch`，以本地引用为准） |
| npm dist-tag 实测 | `latest=0.1.1-rc.2`、`next=0.1.1-rc.2`、`alpha=0.1.2-alpha.5`（代理 http://127.0.0.1:10808，实测成功） |
| 发布版与源码树关系 | 发布版 alpha.5 领先本地源码树（alpha.4）一档；已按 v1 遗留观察点对 alpha.5 发布产物执行锚点抽查（§三），核心契约全部在位 |
| 扩展侧基线 | dsh-vscode-agent v0.1.16，分支 feature/dsh-alpha-compat；探针 tsc exit 0 + `node scripts/sim.mjs` **ALL PASS, 9 SKIP**（与 0.1.16 验收基线一致，本轮零代码改动） |

---

## 零、前置参考

| 参考 | 说明 |
|---|---|
| `docs/archive/dsh-alpha适配分析与方案.md`（alpha.2 原案） | authProxy 方案 A + T/C 双路径 + P1-P11 探针基线；已被 `0.1.14设计方案` 承接 |
| `docs/0.1.17调研报告-dsh-alpha4集成面复验.md`（同题 v1） | alpha.4 复验，8/8 一致或增量兼容，no-adaptation |
| `docs/0.1.17调研报告-dsh-alpha集成面复验-v2.md`（同题 v2） | 独立重取证 + alpha.5 发布产物双锚探针，结论与 v1 一致 |
| `docs/0.1.14设计方案-通道选择与token适配.md` | ADR-29/30：版本阈值判定 V\* = 0.1.2-alpha.2；authProxy 契约 R1-R10 |
| 本轮探针记录 | `.dsh/tmp/architect/probe-log.md`（中间稿，闭环清理） |

**本轮定位（第 1 轮增量复核）**：不照抄 v1/v2 结论，以「当前 0.1.16 实现 ↔ 新 alpha 源码（alpha.4 树）逐项重新取证 + git 历史增量检测（alpha.1 tag → HEAD）+ alpha.5 发布产物独立抽查」三条线重做证据。8 个集成面全部核对，全部一致或增量兼容，零破坏。

> **修订记录 r2（2026-09-02，QA 审计回环）**：按 QA 审计 D4/FAB 修正 §2.1/§2.3/§四 的 git 增量声称——invariant companion 全局清理（commit `15f2997bcb`，`*invariant.ts` 全范围 = 230 files +181/−6241）如实入账；§2.1/§2.3 三处「零变化」改为「仅 invariant companion 删除、契约文件零变化」；§2.4 补记 gateway/connection 切片 client 侧变更；§四收敛口径按实际改写。**结论 no-adaptation 不变、不缩小范围**（方向已由 QA 核证成立：8 集成面 + 6/6 key_changes 逐行属实）。全部修正声称已由 r2 探针重跑命令核对（见 §七与 `.dsh/tmp/architect/probe-log.md`）。

---

## 一、目标/背景分析

dsh 上游发布 alpha 线（browser-auth token 鉴权新契约）后，本扩展已在 0.1.14 完成通道选择 + token 适配（authProxy），0.1.15/0.1.16 完成真机验收修复。用户要求调研分析 dsh 新 alpha 版本的兼容性：新 alpha 最新源码位于同级 deepseek-harness 仓库（当前 master = alpha.4 合并点），若需适配给出设计方案，若无需适配给出有证据支撑的调研报告。

本轮（第 1 轮）是同题的又一次独立重取证：上游相对 v1/v2 取证时点是否有**新的破坏性变化**、发布线（npm dist-tag alpha = 0.1.2-alpha.5）相对源码树是否引入扩展未覆盖的形态——这两点是本轮要回答的核心问题。

## 二、逐项核对（8 集成面 × 证据）

### 2.1 启动契约（CLI 入口 / 命令行参数 / bin）— 一致

| 契约点 | 上游证据（alpha.4 源码） | 扩展侧假设 | 判定 |
|---|---|---|---|
| `web` 子命令 | `apps/cli/src/args.ts:156-169`：`web` = `--profile web` 别名，launcher flags 不变 | `dsh web` 为启动入口 | ✓ |
| `--host/--port/--no-open` | `packages/bundle/web-app/src/startup.ts:51-54`；`--host 0.0.0.0` 显式拒绝（L74-76） | spawn argv `web --host 127.0.0.1 --port N --no-open` | ✓ |
| bin 入口 | `apps/cli/package.json:14-15`：`bin.dsh = lib/bin.js`；name = `@deepseek-ai/dsh` | `dshResolver` 按 name/version 内容匹配 + `lib\bin.js` 存在性 | ✓ |
| `DSH_HOME` | `packages/util/home-paths/src/index.ts:87-91`：configured > `$DSH_HOME` > `~/.dsh` | spawn 注入 `DSH_HOME` 与 `dsh.dshHome` 同源 | ✓ |

**git 增量检测**：`apps/cli/src`、`packages/bundle/web-app/src/startup.ts` 自 `dsh-v0.1.2-alpha.1` tag 到 HEAD（alpha.4）**零变化**（`git diff --stat` 无条目）；`packages/util/home-paths/src` **仅 `invariant.ts` companion 删除**（−30 行），契约文件 `index.ts` 零变化（invariant 性质与全局清理口径见 §2.3）。

### 2.2 端口与就绪判定 — 一致

| 契约点 | 上游证据 | 扩展侧假设 | 判定 |
|---|---|---|---|
| 横幅前缀 | `packages/bundle/web-app/src/index.ts:280`：`console.log(\`dsh web: ${authenticatedUrl}… (LAN: …)\`)`；`authenticatedUrl` = 根 URL + `?token=`（`browser-auth.ts:223-230`） | `URL_RE = /dsh web:\s+(http:\/\/127\.0\.0\.1:(\d+))/i` 端口解析 + `TOKEN_RE` token 捕获 | ✓ |
| 默认端口/主机 | `cordis.patch.yml:120-121`：`host ?? '127.0.0.1'`、`port ?? 3080` | `dsh.port` 默认 3080 | ✓ |
| 就绪判定 | index 先过 `authorizeIndex`（`frontend-static/src/index.ts:88-91`）→ 200 含 `__DSH_BOOT__`；token 契约下 `GET /?token=` → 303 | `probeDetail` 三态（200+BOOT / 303 / 401） | ✓ |

**git 增量**：`web-app/src/index.ts` 仅 3 行（systemPrompt section order 取值方式，与面板无关）；`cordis.patch.yml` +12/−6 = 18 行、仅插件表增删（`session-turn-outline`、`ui-schedule` 新增、`tool-subagent-report` 注释清理），webserver host/port 行零变化（r2 逐行核对 diff 体，无 host/port 触碰）；同切片 `web-app/src/invariant.ts` companion 删除（−30 行，见 §2.3）。

### 2.3 `__DSH_BOOT__` 注入与 iframe 直连/CSP — 一致

- 注入行仍在 index：`packages/client/modules/src/index.ts:522`（`rows.push({kind:'global', name:'__DSH_BOOT__', …})`）经 `packages/host/webserver/src/injections.ts:49-57` 渲染为 `<script>globalThis["__DSH_BOOT__"] = …</script>`（head，需凭据可见——鉴权前置不变）。
- **additive**：injections.ts 新增 `__DSH_BOOT_READY__` deferred（L79-86、L104），为页面内部启动就绪信号；扩展探测只搜 `__DSH_BOOT__` 字符串，仍命中，无影响。
- **git 增量**：`client/modules/src/index.ts` 零变化（`git diff --stat` 无条目）；`webserver/src`、`frontend-static/src` 各**仅 `invariant.ts` companion 删除**（−59 / −34 行），契约文件零变化。
- **invariant companion 全局清理（r2 修正入账，QA D4/FAB）**：`*invariant.ts` 在 tag→HEAD 全范围共 **230 files +181/−6241**（复现命令与输出见 §七；清理提交 `15f2997bcb`「cleanup: omit unneeded invariant companions」本身 = 1407 files +1986/−11398，因同批清理各包对 companion 的 import 引用行，并另有少数保留 invariant.ts 的 M 调整）。被删文件自述为「Package-owned invariant companion」（Cordis dev-assert 诊断插件，非运行时契约）。对本扩展直接相关切片的删除量：home-paths −30 / web-app −30 / webserver −59 / frontend-static −34 / connection −34 / credentials-local −31 / gateway −31。全部 `*invariant.ts` diff 内扩展敏感字符串（`__DSH_BOOT__`/`dsh-auth-`/`remote.mux`/`browser-session`/`credentials.yaml`）仅 1 处命中，且为**被删除的 JSDoc 注释行**（`packages/client/connection/src/invariant.ts`，描述为何此处无 runtime invariant）——对扩展零影响。
- 扩展侧 CSP `frame-src http://127.0.0.1:*` 与 authProxy 同 host-source 假设不受上游影响（iframe 语义不变）。

### 2.4 alpha 通道 token 鉴权与本地代理（authProxy / launchInfo）— 一致（含 2 处 additive）

| 契约点 | 上游证据（alpha.4） | 与 v1 基线（alpha.2）对照 | 判定 |
|---|---|---|---|
| Cookie v1 格式 | `browser-auth.ts:106-132`：`dsh-auth-<b64url(sha256(authority))>` = `v1.<payload>.<hmac>`，payload `{version:1, authority, issuedAt, expiresAt}` | **零变化**（该文件自 alpha.1 起 git diff 无条目） | ✓ |
| Cookie 属性 | `browser-auth.ts:121-123`：`HttpOnly; SameSite=Strict; Max-Age; Path=/`（非 Secure）；30 天（`connection/src/index.ts:88` default） | 零变化 | ✓ |
| token 交换 | `browser-auth.ts:240-265`：`GET /?token=`（GET、`/`、单 token、Host 可解析）→ `303 + Set-Cookie` 到干净 `/` | 零变化 | ✓ |
| 无凭据响应 | `browser-auth.ts:304-312`：401，正文 `dsh web authentication required; reopen the URL printed by dsh web.` | 零变化 | ✓ |
| 栅栏顺序 | `rpc-host.ts:96-98`：`requestRejection` = trust fence 403 → cookie auth 401；`api-request-trust.ts`：Host 必须回环（127/8、localhost、[::1]）或 trustedHosts、`sec-fetch-site: cross-site` 拒绝（L106）、Origin 必须等于 Host authority（L111-117） | 栅栏顺序零变化（`rpc-host.ts` 全文件仅 4 行 = 错误码字符串 ×2，authProxy 不解析、透传，见 git 增量；authProxy R3/R4/R6/R8 假设成立） | ✓ |
| `/api` 与 WS | `connection/src/index.ts:114-127`（prefix `/api`，新单点 `api-path.ts` 值不变）；`api/gateway/src/index.ts:212-228`（`/api/remote.mux` upgrade → `requestRejection`） | `/api` prefix 与 upgrade 鉴权段零变化（gateway `index.ts` 全文件 141 行 diff = 错误码/心跳/重构，鉴权/upgrade 段在其中零触碰，见 git 增量） | ✓ |
| 凭据库 | `credentials-local/src/index.ts`：`<DSH_HOME>/.credentials.yaml`（L61）、`records/<scope>/<id>` + `kind: grant` + `payload`（L289-357）、0600；记录键 `client-connection/browser-session` | 零变化（authProxy `readCredentialsSecret` 解析假设成立） | ✓ |
| **additive ①** | `browser-auth.ts:267-275` 新分支：带 token 且已持有效 cookie → 303 到 `/`（不再 mint 新 cookie） | 扩展 T 路径 bootstrap 只需首次 303+Set-Cookie，无影响 | ✓ |
| **additive ②（行为级）** | `api/gateway/src/index.ts`：WS 心跳默认 `30_000ms` → `2_000ms`（alpha.1→HEAD diff） | authProxy WS 隧道为原始 socket 双向 pipe、无超时逻辑，心跳帧透传；仅代理上心跳帧频率升高（每连接每 2s 一帧，量级可忽略）。列为联调观察点 | ✓ |

**git 增量**：`api/gateway/src/index.ts` 141 行 diff = 错误码 `gateway/` 命名空间化 + WS 心跳默认值 + RemoteError/Deque 重构；鉴权/upgrade 段零变化。同切片另有 `remote-error-codes.ts` 新增（35 行）、`types.ts`（34 行）、`stream-server.ts`（18 行）与 `client/` 侧 5 文件（stream-client 144、client/index 75、journal-stream 38、remote-stream 29、snapshot-stream 10）流客户端重构。`connection/src` 切片：`rpc-host.ts` 4 行 = RPC 错误码字符串 ×2（authProxy 不解析、透传）；`browser-auth.ts`、`connection/src/index.ts`、`api-request-trust.ts` 零变化；另有 `client/` 侧 5 文件（connection.ts 132、fixture.ts 99、client/index.ts 79、rpc.ts −29、api.ts −2）前端连接逻辑重构。上述两侧 client/ 变更与 `remote-error-codes/types/stream-server` 经扩展敏感字符串扫描（`__DSH_BOOT__`/`dsh-auth-`/`remote.mux`/`browser-session`/`credentials.yaml`）**命中 0**——均为 web 前端客户端/错误码词汇层，不在扩展依赖面。两切片的 `invariant.ts` companion 删除见 §2.3（gateway −31 / connection −34）。

### 2.5 外部实例接管（进程命令行特征）— 一致

- alpha.5 发布产物实际进程命令行 = `node <npx-cache>\…\@deepseek-ai\dsh\lib\bin.js web …`（bin 入口仍 `lib/bin.js`，见 §三），命中扩展 `looksLikeDsh`（`instance.ts:516`）三特征之 `/dsh[\/](lib[\/])?bin\.(js|mjs)/`；`@deepseek-ai/dsh` 与 `--profile web` 特征仍覆盖用户手输命令形态。
- netstat/CIM 链路（`resolvePortPid` / `processCommandLine`）为扩展侧实现，不受上游影响。

### 2.6 数据目录与注册表边界 — 无影响

- 扩展数据目录 `%LOCALAPPDATA%\DshVscode`（`paths.ts dataDir()`）自持 instance.json/锁/日志，与 dsh 侧 DSH_HOME（`~/.dsh`，`.credentials.yaml` 在其下）分工不变（home-paths 语义零变化）。
- 注册表 `instance.json` schema 未动（本轮扩展侧零代码改动）。

### 2.7 webview 面板依赖的页面结构/消息协议 — 无影响

- 扩展面板 = iframe 隔离 + 自研消息协议（`panelMessages.ts`：webviewReady/stateAck/showDetails/openBrowser/restart/stop/chooseChannel），不解析 dsh DOM/路由；对 dsh 的唯一契约面 = index 含 `__DSH_BOOT__` + `/api` + `/api/remote.mux` + `/plugins/*` 公开（`client/modules/src/index.ts:586`）——§2.3/§2.4 已核，全部在位。
- `webviewDetails.ts` 为纯渲染层（grep import 证实：vscode + runtime + webviewHtml + channelSelect + paths），无 dsh 侧契约依赖。

### 2.8 npm dist-tag 现状（best-effort 实测成功）— 与通道模型一致

- 实测：`latest=0.1.1-rc.2`、`next=0.1.1-rc.2`、`alpha=0.1.2-alpha.5`。
- 上游发布脚本 `scripts/release/families.ts` `DshFamily.distTagForVersion`：prerelease 通道段 `alpha`/`canary` → 同名 dist-tag，其他（rc）→ `next`，稳定 → `latest`——与 0.1.14 设计 A6/通道模型（`dsh.channel` = latest/next/alpha 三个 dist-tag）一致。
- 本机 npx 缓存：`ebf017b61addb8bd` @ 0.1.2-alpha.5（alpha 通道产物）、`b86ed90107c62dab`/`1e7f6d9597241db0` @ 0.1.1-rc.2（latest）、`2ede61d9d1d3d32e` @ 0.1.0-rc.7。扩展 `dshResolver` 按 name/version 内容匹配任意缓存目录，与多版本共存形态兼容。

## 三、alpha.5 发布产物抽查（v1 遗留观察点执行）

**包结构变化（本轮新发现，需如实记录）**：`@deepseek-ai/dsh` 0.1.2-alpha.5 的 npm 发布包从「单包内嵌全部逻辑」重构为**薄 CLI 入口 + 按包拆分的 `@deepseek-ai/dsh-*` 依赖包**：

- `lib/` 仅 5 个 chunk（bin.js 8KB = args 解析 + profile boot，与源码 `apps/cli/src/args.ts` 同构，`web` 别名在）；`files: ["lib/*.js"]`、`bin.dsh = lib/bin.js` 不变。
- web 逻辑拆入依赖包：`dsh-web-app`、`dsh-client-connection`、`dsh-api-gateway`、`dsh-host-webserver`、`dsh-host-frontend-static`、`dsh-credentials-local`、`dsh-home-paths` 等。

**锚点抽查**（对依赖包做原始字符串检查，按职责分布全部命中）：

| 依赖包 | 锚点 | 结果 |
|---|---|---|
| `dsh-web-app` | `dsh web:`（横幅）、`__DSH_BOOT__` | ✓ / ✓ |
| `dsh-client-connection` | `dsh-auth-`、`browser-session`、`SameSite=Strict` | ✓ / ✓ / ✓ |
| `dsh-api-gateway` | `remote.mux` | ✓ |
| `dsh-credentials-local` | `.credentials.yaml` | ✓ |

**对扩展的影响评估：零破坏。** `dshResolver` 只消费 `@deepseek-ai/dsh/package.json` 的 name/version + `lib\bin.js` 存在性；spawn 走 bin.js 入口；进程特征照旧命中——packaging 拆分对扩展完全透明。

## 四、结论（明确判定）

**无需适配（no-adaptation），置信度：高。**

1. 8 个集成面（启动契约、端口与就绪、`__DSH_BOOT__`/CSP、token 鉴权与代理、外部接管、数据目录、webview 依赖面、dist-tag）逐项对照 alpha.4 源码重新取证，全部**一致或增量兼容**，零破坏。
2. git 增量检测（`dsh-v0.1.2-alpha.1` → HEAD）在扩展契约面切片内的实况 = **invariant companion 全局清理 1 批 + 非 invariant 实质变更 17 文件**。前者：`*invariant.ts` = 230 files +181/−6241，纯 dev-assert 诊断插件删除（扩展敏感字符串仅 1 处被删注释行命中，零契约影响，§2.3）。后者按语义可枚举为 5 个扩展相关点——gateway 错误码命名空间化 + WS 心跳默认 30s→2s（index.ts 141 行，伴 remote-error-codes.ts/types.ts/stream-server.ts）、rpc-host 错误码字符串 ×2（4 行）、web-app systemPrompt order 取值（3 行）、cordis.patch.yml 插件表增删（18 行）、外加 gateway/connection 两侧 client/ 共 10 文件 web 前端流逻辑重构（扩展敏感字符串命中 0）——**全部非破坏**（透传语义或与扩展无关）。
3. alpha.5 发布产物（npm alpha dist-tag 当前指向）虽发生 packaging 拆分，但 bin 入口、子命令、鉴权锚点、横幅、boot 注入、凭据文件格式全部在位，扩展解析面（name/version + lib\bin.js + 进程特征）不受影响。
4. 扩展工作区健康：tsc exit 0 + sim ALL PASS, 9 SKIP（440 PASS 基线复跑，零代码改动）。

## 五、风险与后续观察点

| # | 项 | 等级 | 说明 / 建议 |
|---|---|---|---|
| O-1 | **alpha.5 源码与本地源码树不完全同源** | 低 | 本地仓库止步 alpha.4（merge 4e84901e64），alpha.5 为发布产物、无对应本地源码；其增量已由锚点抽查覆盖核心契约，但若 alpha.5 含未及锚点的行为变化，需待上游同步后复验（下游 v1/v2 同此结论，风险未扩大）。 |
| O-2 | **WS 心跳默认 30s→2s** | 低 | authProxy 隧道无超时逻辑、纯透传；真机联调时观察长连接（`/api/remote.mux`）经代理的稳定性与空闲回收行为即可，无需设计变更。 |
| O-3 | **packaging 拆分趋势** | 低 | 上游若继续细化依赖包拆分（甚至改 bin 布局），`dshResolver` 的 `lib\bin.js` 假设是唯一敏感点；现状（alpha.5）已验证不变，列入后续 alpha 版本的例行核对项。 |
| O-4 | **`__DSH_BOOT_READY__` deferred** | 记录 | additive 页面内部信号；扩展探测不受影响，仅登记备忘。 |
| O-5 | **上游 master 是否已有超-alpha.4 提交** | 低 | 本轮只读约束未 fetch；建议编排者/用户在联网环境复跑 `git -C deepseek-harness fetch` 后对账（若远端无新提交则 O-1 一并消除）。 |

## 六、决策记录

| # | 决策 | 理由 |
|---|---|---|
| D1 | 结论 no-adaptation，本轮零代码改动 | 8 集成面零破坏证据（§二/§三）；探针全绿（tsc 0 + sim ALL PASS, 9 SKIP） |
| D2 | 取证方法 = 「源码逐项复核 + git tag→HEAD 增量 diff + 发布产物锚点抽查」三线并行 | git 增量检测能以最小探针成本枚举全部变化面，避免整目录通读；产物抽查兑现 v1 遗留观察点（alpha.5） |
| D3 | 文档命名沿用同题多版本 `-v3-`（`0.1.17调研报告-dsh-alpha集成面复验-v3.md`） | docs/README.md 目录治理公式（`<0.1.x><类型词>-<主题>-vN-`）；v1/v2 已占 0.1.17 同题前两档 |
| D4 | 不更新 `0.1.14设计方案` / `联调测试剧本` | 本轮无任何行为/剧本变化；O-2 观察点通过本报告 §五 传达即可 |

## 七、证据清单

- 探针记录（含全部 file:line、git diff stat、锚点原始数据）：`.dsh/tmp/architect/probe-log.md`（r2 重写，含本轮对账复现）
- 工作区探针：`node node_modules\typescript\bin\tsc -p tsconfig.json` → exit 0；`node scripts/sim.mjs`（`DSH_VSCODE_DATA_DIR` 指向 `.dsh/tmp/architect/sim-data`）→ **ALL PASS, 9 SKIP**（0.1.16 验收基线形态复现）
- npm dist-tag 实测命令：`npm view @deepseek-ai/dsh dist-tags --json`（代理 http://127.0.0.1:10808，成功）
- 上游 git 查询（只读）：`git -C D:/working/projects/deepseek-harness log -1 / diff --stat dsh-v0.1.2-alpha.1..HEAD / tag -l`
- r2 QA 对账复现（只读，输出与本报告修订声称逐一核对）：`git -C deepseek-harness diff --stat dsh-v0.1.2-alpha.1..HEAD -- '*invariant.ts'` → **230 files changed, 181 insertions(+), 6241 deletions(-)**（与 QA evidence 口径一致）；`git show --stat 15f2997bcb` → 1407 files, +1986/−11398；切片复现：home-paths `invariant.ts|30−`、frontend-static `invariant.ts|34−`、webserver `invariant.ts|59−`、connection `invariant.ts|34−`、credentials-local `invariant.ts|31−`、gateway `invariant.ts|31−`、web-app index.ts 3 行、cordis.patch.yml +12/−6、gateway index.ts 141 行、rpc-host.ts 4 行；`apps/cli/src`、`client/modules/src/index.ts`、`web-app/src/startup.ts`、`browser-auth.ts`、`connection/src/index.ts`、`api-request-trust.ts` 无条目（零变化）

> 评审人：待定 | 状态：待终审 | 日期：2026-09-02
