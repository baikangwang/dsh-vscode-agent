# 0.1.17 调研报告：dsh alpha 集成点复验 v2（结论：无需适配）

> 状态：**待评审**（调研结论 no-adaptation，零代码改动轮）｜版本归属：0.1.17（复验载体）｜日期：2026-09-02
>
> 来源锚点（事实基准，逐项独立取证，不沿用任何既有文档结论）：
> - 上游源码：`D:/working/projects/deepseek-harness`（严格只读）· 分支 `master` · HEAD `4e84901e6471b79ec0338099867ebb4606d12bb5`（2026-09-01 23:37:26 +0800，与 `origin/master` 一致）· describe `dsh-v0.1.2-alpha.1-648-g4e84901e64` · `apps/cli` 版本 **0.1.2-alpha.4**（`apps/cli/package.json` L4）· 工作树干净（`git status --porcelain` 零行）。
> - **发布线（本轮新增维度）**：npm dist-tag 实测 `{"next":"0.1.1-rc.2","latest":"0.1.1-rc.2","alpha":"0.1.2-alpha.5"}`；**发布版 0.1.2-alpha.5 编译产物已在本机 npx 缓存**（`%LOCALAPPDATA%\npm-cache\_npx\ebf017b61addb8bd`，即当前 DSH 运行时自身），本轮对其产物做契约常量探针（§3）——源码树锚（alpha.4）与发布产物锚（alpha.5）双锚核对。
> - 本扩展：v0.1.16 · 分支 `feature/dsh-alpha-compat` · HEAD `f1af0d4`。
> - 对比基线（仅作参照）：`docs/archive/dsh-alpha适配分析与方案.md`（alpha.2 时代原案）、`docs/0.1.17调研报告-dsh-alpha4集成点复验.md`（同日先行工作稿，未提交；本轮为同题 v2，独立重取证，见 §5 决策 2）。

## 总结论

**no-adaptation：8 个集成点在 alpha.4 源码树下逐项独立复核全部与 v0.1.14–0.1.16 实现锚点一致或增量兼容；且发布版 0.1.2-alpha.5 产物（用户实际运行的版本）契约常量逐项命中、零框架阻断头命中——源码锚与发布锚双锚一致，零破坏性变更。v0.1.16 无需为 alpha 线发起适配设计。** 契约判定锚（按版本阈值 `TOKEN_AUTH_MIN_VERSION='0.1.2-alpha.2'`，不按渠道）维持不变。

## §1 验证范围与方法

- 逐项对照：alpha.4 源码证据（file:line，本轮全部独立读取核实）× 扩展侧契约锚点（`src/` 现行实现，grep 定位 + 关键段精读）。
- **本轮相对 v1 的增量**：① 全部 8 面重新独立取证（不复制 v1 结论）；② 新增「发布版产物双锚探针」维度——对 npx 缓存中实际运行的 0.1.2-alpha.5 编译产物做契约常量核对（v1 仅实测 dist-tag 元数据，未核对产物）；③ dist-tags 复测确认发布线无新版本。
- 探针：`tsc` 编译、`scripts/sim.mjs` 无头回归、`npm view` dist-tag 线上实测、产物 grep 探针（记录见 §4）。
- 沙箱约束：deepseek-harness 严格只读；npm 探针使用工作区本地缓存目录；sim 输出 `*>` 落盘后读取。

## §2 八面逐项证据（本轮独立取证，file:line）

### 面 1：CLI 入口/参数 —— 一致 ✅

- 上游：`apps/cli/package.json` bin `dsh = lib/bin.js`（L14-16）、版本 0.1.2-alpha.4（L4）、包名 `@deepseek-ai/dsh`（L2）。`apps/cli/src/args.ts` L13 注释 + L156 `program.command('web')`：`web` 为 `--profile web` 的硬编码别名。`packages/bundle/web-app/src/startup.ts` L51-54 web 参数族 `--host/--no-open/--port/--trusted-host`；L74-76 `--host 0.0.0.0` 显式拒绝；L77-79 `--port` 必须数字。
- 环境处理：`apps/cli/src/profile-boot.ts` L64-66 注释明言 `$DSH_HOME` 为 launcher/test 在 import 后设置的注入点；L34/L258 内部参数经 `provideCmdline` 以 `ctx.cmdlineArgs` 快照传入树内。新增（增量）：L76/L96/L170 `DSH_TELEMETRY_DISABLED` 环境开关（任意非空值禁用会话遥测）——可选加固旋钮，扩展不设置亦不受影响；L221-224 `SIGTERM` → exit 0（监督者正常停止语义，Windows 侧扩展走 taskkill 路径，无涉）。
- 扩展锚点：`src/dshProcess.ts` spawn argv `node <binJs> web --host 127.0.0.1 --port N --no-open` —— 与 web 参数族逐字吻合（`--trusted-host` 为可选，不传即默认行为）；`src/dshResolver.ts` L27 `DSH_PKG='@deepseek-ai/dsh'`。
- 结论：解析与 spawn 假设全部成立。

### 面 2：端口/就绪横幅 —— 一致 ✅

- 上游：`packages/bundle/web-app/src/index.ts` L80 `LOOPBACK_HOST='127.0.0.1'`；L158-161 `localWebUrl()` 产出 `http://127.0.0.1:<port>`；L268-289 `announceReady()`，L280 `console.log(\`dsh web: ${authenticatedUrl}${lanUrl…}\`)`；`authenticatedUrl` 来自 `packages/client/connection/src/browser-auth.ts` L223-230（pathname 重置 `/`、清空 search/hash、`searchParams.set('token', launchToken)`）→ 横幅精确形态 `dsh web: http://127.0.0.1:<port>/?token=<43位base64url> (LAN: …)`（token 43 字符佐证：`apps/cli/tests/web-auth.e2e.ts` L166 正则 `^[A-Za-z0-9_-]{43}$`，本轮独立核实）。
- 扩展锚点：`src/dshProcess.ts` L424 `URL_RE = /dsh web:\s+(http:\/\/127\.0\.0\.1:(\d+))/i` + L437 `TOKEN_RE = /\?token=([A-Za-z0-9_-]+)(?=\s|$)/` —— 与 L280 产出逐段匹配；e2e L94 横幅正则 `/dsh web: (http:\/\/[^\s]+)/u` 独立佐证。
- 备注：横幅 URL 恒以 `127.0.0.1` 打印（`localWebUrl` 硬编码），即便用户新增 `$DSH_HOME/cordis.patch.yml` 家庭层覆盖 webserver host，横幅解析仍健壮；LAN 段仅作扩展展示。

### 面 3：`__DSH_BOOT__` 注入 + iframe/CSP —— 一致（增量兼容）✅

- 上游：注入为结构化 `IndexInjection` 行——`packages/client/modules/src/index.ts` L489 `bootInjections()`、L522 `rows.push({kind:'global', name:'__DSH_BOOT__', value:graph})`；渲染端 `packages/host/webserver/src/injections.ts` L56（global 行渲染 `globalThis["__DSH_BOOT__"] = …`）；L86 新增 `__DSH_BOOT_READY__` deferred tail 行（纯增量，不参与扩展判据）。
- 框架阻断排查（本轮 grep）：`packages/host/webserver/src`、`packages/host/frontend-static/src`、`packages/client/connection/src` 三包源码零 `X-Frame-Options`/`frame-ancestors`/`Content-Security-Policy`/`Cross-Origin-*` 头命中 —— iframe 嵌入不受阻。
- 路由范围：`API_PATH='/api'`（`api-path.ts` L7）；`rpc-host.ts` L96-103 拒绝序 = 信任防护 403 → cookie 401（正文 `'dsh web authentication required; reopen the URL printed by dsh web.\n'`，browser-auth.ts L304-312）；`REMOTE_STREAM_MUX_PATH='/api/remote.mux'`（`api/gateway/src/stream-protocol.ts` L6）精确路径 upgrade 注册（`gateway/src/index.ts` L213、`webserver/src/index.ts` L175-185/L257-280）；`frontend-static/src/index.ts` L88-95 仅索引页受 `authorizeIndex`，静态资源公开 —— 与 `src/authProxy.ts` R1-R10 转发规则及 401 偏差检测假设一致。
- 扩展锚点：探活判据 `text.includes('__DSH_BOOT__')`（`src/dshProcess.ts` L1068）—— substring 判据对结构化行天然兼容；`src/webviewHtml.ts` L66 CSP `frame-src http://127.0.0.1:*` 覆盖代理端口（`127.0.0.1:0` OS 分配）。

### 面 4：token 认证与本地代理协议 —— 一致 ✅

- 上游常量（`browser-auth.ts` 全文 313 行，本轮逐段核实）：`COOKIE_PREFIX='dsh-auth-'`（L16）、`COOKIE_PAYLOAD_VERSION=1`（L17）、`STORED_SECRET_VERSION=1`（L18）、`SECRET_BYTES=32`（L14）、`TOKEN_QUERY='token'`（L15）、凭据键 `credentialKey('client-connection','browser-session')`（L12）；launchToken 为进程内 WeakMap（L20、L52-58，每启动随机、不落盘）；cookie 名 = `'dsh-auth-' + base64url(sha256(authority))`（L106-108）；cookie 值 = `v1.<body>.<sig>` HMAC-SHA256（L129-132）；sessionCookie 属性 `HttpOnly; SameSite=Strict`、非 Secure（L121-123）；30 天默认生命周期（`connection/src/index.ts` L88/L102 `cookieMaxAgeDays` 默认 30；`cordis.patch.yml` L162-169 connection 行仅注入 `trustedHosts`，无 cookieMaxAgeDays 覆盖）；信任防护 `sec-fetch-site: cross-site` 拒绝（`api-request-trust.ts` L106），403 先于 401。
- 鉴权流（`authorizeIndex` L240-282）：带 token 的 `GET /` → 校验后 `303` + `Set-Cookie`（location 干净 `/`）；带有效 cookie → 303 清参；无凭据 → 401。secret 持久化（`initializeSecret` L161-178，`credentials.modifyRecord` 仅在无记录时写入）。
- 凭据文件：`packages/credentials/credentials-local/src/index.ts` L167 `DOCUMENT_VERSION=1`、L222 顶层键 `version/refs/records`、L699 等写入 `0o600`。
- e2e 佐证（`apps/cli/tests/web-auth.e2e.ts` L160-199，本轮独立核实）：L168-171 伪造 Host（localhost≠127.0.0.1）→ 401（authority 绑定生效）；L173-181 token 交换 → 303 + `Set-Cookie`（HttpOnly、SameSite=Strict、非 Secure）；L183-190 携带 cookie 访问 `/api` → 200；L194-197 **每次启动令牌重新生成**、跨重启旧 cookie 依然 200（secret 持久化生效）。
- 主目录：`packages/util/home-paths/src/index.ts` L79-91 优先级 配置 > `$DSH_HOME` > `~/.dsh` 不变。
- 扩展锚点：`src/authProxy.ts` L89-97 常量组、L110-112 `dshCookieName`（与 L106-108 同构）、L126-142 `mintSessionCookie`（与 L129-132 逐字段同构）、L152+ `readCredentialsSecret`（只读 `.credentials.yaml`，缩进遍历 records → browser-session → payload → secret）；`src/dshProcess.ts` L471 `TOKEN_AUTH_MIN_VERSION='0.1.2-alpha.2'`（版本阈值判定，L538-546 纯函数）；`src/runtime.ts` L522-676 contract tier 判定 + T/C 双路径编排 + 矛盾事实 WARN（L631/L654）。
- 结论：T 路径（横幅 token 交换）与 C 路径（凭据库本地生成）所需的上游契约全部在位且与 0.1.14 设计逐字段吻合。

### 面 5：外部实例接管 —— 一致 ✅

- 上游：托管启动命令行形态不变（`@deepseek-ai/dsh/lib/bin.js` + `web` 别名，见面 1）；`dsh web` 直跑同样经 bin.js。
- 扩展锚点：`src/instance.ts` L514-516 `looksLikeDsh` 三规则（`@deepseek-ai/dsh` 路径、`--profile web` 字面、`dsh[\/](lib[\/])?bin.(js|mjs)` 路径形态）—— 对 npx 缓存目录、全局 shim、`dsh web` 直跑形态均保持命中；`src/runtime.ts` L413 接管探测处调用不变。
- 备注：`--profile web` 字面规则仅对显式 profile 写法命中（alpha.2 时代即如此，非本轮回归）。

### 面 6：数据目录边界 —— 一致 ✅

- 上游：dsh 侧数据（会话、凭据 `$DSH_HOME/.credentials.yaml`、profile 目录、家庭层 `$DSH_HOME/cordis.patch.yml`）全部锚定 `DSH_HOME`（默认 `~/.dsh`，home-paths L12/L18/L79-91）；无新增越界目录。
- 扩展锚点：`src/paths.ts` 数据目录 `%LOCALAPPDATA%\DshVscode`（`DSH_VSCODE_DATA_DIR` 衔接点不变）—— 两域边界维持；扩展读 `.credentials.yaml` 仅作只读旁路（authProxy 本地生成 cookie），绝不写回 dsh 域。

### 面 7：Web UI 面（面板协议/静态服务）—— 一致 ✅

- 面板上行白名单（`src/panelMessages.ts` L2 纯 Node、无 'vscode'/dsh DOM 依赖）、详情卡动作（`src/webviewDetails.ts`）、iframe CSP（`src/webviewHtml.ts`）均为扩展内部契约，不依赖 dsh Web UI DOM —— Web UI 侧变更仅经 URL/iframe 表面化，已被面 2/3 覆盖。
- 服务端增量：webserver `compression` 配置化（`webserver/src/index.ts` L58-79 默认 `'none'`；L128-130 校验；`cordis.patch.yml` L116-124 web profile 设 gzip level 1 / threshold 1024）—— authProxy 将 `Content-Encoding` 按实体头原样透传，兼容（发布产物探针 §3 佐证 gzip 中间件在位）。
- 静态服务：`frontend-static/src/index.ts` L88-95 仅 index 过鉴权、静态文件公开；fallback 仅 GET/HEAD（L124-131）—— 代理透传面不变。

### 面 8：npm 通道状态 —— 一致 ✅（线上实测 + 产物核对）

- 本轮实测 `npm view @deepseek-ai/dsh dist-tags --json`（代理 127.0.0.1:10808 + 工作区缓存）：`{"next":"0.1.1-rc.2","latest":"0.1.1-rc.2","alpha":"0.1.2-alpha.5"}` —— 与先行稿 v1 记录一致，**发布线无新版本**。
- 渠道→标签映射：`scripts/release/families.ts` L355（`alpha`/`canary` 渠道以自名 dist-tag 发布）、L294（其余预发布 → `next`）、L324（tagPrefix `dsh-v`）—— 与扩展 `dsh.channel` 枚举（`src/channelSelect.ts` L26 `['latest','next','alpha']`）吻合；旧值 `preview` 非 dist-tag 的归一化逻辑仍正确。
- 契约判定按版本（`TOKEN_AUTH_MIN_VERSION='0.1.2-alpha.2'`），alpha.5 亦满足阈值；渠道标签仅作安装通道，不参与判定 —— 设计维持有效。
- **发布产物核对（新增）**：见 §3。

## §3 发布版 0.1.2-alpha.5 产物双锚探针（本轮新增维度）

对象：`%LOCALAPPDATA%\npm-cache\_npx\ebf017b61addb8bd\node_modules\@deepseek-ai\`（`dsh` CLI 包版本 0.1.2-alpha.5，即当前本机 DSH 运行时自身）。方法：对编译产物 `lib/*.js` 批量 Select-String 契约常量。

| 探针 | 目标包 | 结果 | 与源码树锚对照 |
|---|---|---|---|
| `'dsh-auth-'`（cookie 前缀） | dsh-client-connection lib/index.js:200 | HIT | = browser-auth.ts L16 |
| `browser-session`（凭据键） | dsh-client-connection lib/index.js:195 | HIT | = browser-auth.ts L12 |
| 401 正文 `dsh web authentication required` | dsh-client-connection lib/index.js:424 | HIT | = browser-auth.ts L311 |
| `cookieMaxAgeDays`（默认 30） | dsh-client-connection lib/index.js:329 | HIT | = connection/index.ts L88 |
| `sec-fetch-site`（信任栅栏） | dsh-client-connection lib/index.js:184 | HIT | = api-request-trust.ts L106 |
| 横幅 `dsh web: ` | dsh-web-app lib/index.js:211 | HIT | = web-app/index.ts L280 |
| `127.0.0.1`（回环锚） | dsh-web-app lib/index.js:41 + dsh-host-webserver lib/index.js:141 | HIT | = index.ts L80 |
| `/api/remote.mux` | dsh-api-gateway lib/types/stream-protocol.js:3 | HIT | = stream-protocol.ts L6 |
| `DSH_HOME` | dsh-home-paths lib/index.js:11 + dsh/lib/profile-boot-*.js | HIT | = home-paths L18 |
| `.dsh`（默认 home） | dsh/lib/profile-boot-*.js:203 | HIT | = home-paths L12 |
| `__DSH_BOOT__` | dsh-client-modules lib/index.js:34 | HIT | = modules/index.ts L522 |
| `__DSH_BOOT_READY__` | dsh-host-webserver lib/index.js:58 | HIT | = injections.ts L86 |
| `refs`/`records`（凭据布局） | dsh-credentials-local lib/index.js:71 | HIT | = credentials-local L222 |
| `X-Frame-Options` | 全包 | **零命中（预期）** | 框架不阻断 |
| `frame-ancestors` | 全包 | **零命中（预期）** | 框架不阻断 |
| `Content-Security-Policy` | 全包 | **零命中（预期）** | 框架不阻断 |

**结论：发布版 alpha.5 产物与 alpha.4 源码树契约逐项一致（16 项命中 + 3 项预期零命中）。源码锚（本轮逐行取证）与发布锚（产物探针）双锚互证，扩展所依赖的全部契约条目在用户实际运行的版本上成立。**

## §4 探针记录

| 探针 | 命令/方式 | 结果 |
|---|---|---|
| ① 类型编译 | `node node_modules\typescript\bin\tsc -p tsconfig.json` | **exit 0** |
| ② 无头回归 | `node scripts/sim.mjs`（`DSH_VSCODE_DATA_DIR` 指向工作区临时目录；输出 `*>` 落盘 `.dsh/tmp/architect/sim-run.log`） | **exit 0，ALL PASS，9 SKIP**（SKIP 为套件头部声明的沙箱受限实机项：netstat/taskkill/detached 树验证） |
| ③ npm dist-tag | `npm view @deepseek-ai/dsh dist-tags --json`（代理 127.0.0.1:10808，`--cache` 指向 `.dsh/tmp/architect/npmcache`） | latest/next=0.1.1-rc.2，**alpha=0.1.2-alpha.5**（与先行稿记录一致，无新发布） |
| ④ 发布产物契约探针 | 对 npx 缓存 `ebf017b61addb8bd`（0.1.2-alpha.5）产物批量 Select-String | 16 项命中 + 3 项预期零命中（§3 表） |
| ⑤ 上游 e2e 场景核实 | `apps/cli/tests/web-auth.e2e.ts` L160-199 精读 | token 43 位 / 303+Set-Cookie / authority 绑定 401 / 跨重启 cookie 存续 —— 全部与扩展三态探测器对齐 |
| ⑥ 两仓库状态 | `git -C … log -1` / `status --porcelain` | harness master@4e84901e 工作树干净；扩展 f1af0d4（先行稿 v1 为未提交工作稿，见 §5-2） |

中间产物：`.dsh/tmp/architect/`（sim-run.log、npmcache/、simdata/，闭环时随临时产物清理）。

## §5 决策记录

1. **不发起适配轮**：8/8 面 + 发布产物双锚证据表明 alpha 线（源码 0.1.2-alpha.4、发布 0.1.2-alpha.5）对 v0.1.16 无破坏；按「零破坏不设版本轮」原则，0.1.17 不因本调研产生代码改动。
2. **本稿命名 = 同题多版本 v2**：`0.1.17调研报告-dsh-alpha4集成点复验.md`（先行稿，同日产出、git 未提交）已占用 0.1.17 复验主题；按 `目录治理设计方案.md` §4.0.1 同题多版本 `-vN-` 公式，本稿以 `-v2` 续接，版本归属仍 0.1.17。两稿关系：本稿为独立重取证 + 新增发布产物维度，不沿用先行稿结论；闭环时随 A1 链一并提交，用户评审可裁定保留/合并。
3. **契约判定锚维持**：`TOKEN_AUTH_MIN_VERSION='0.1.2-alpha.2'` 按版本判定继续有效（alpha.5 亦满足）；不引入按渠道/按 dist-tag 的判定。
4. **先行稿 v1 §5-1 观察点执行完毕**：其建议「alpha.5 拉取后按同法抽查增量」——本稿 §3 即该抽查的落地（发布产物已在本机，探针完成，结论一致）。
5. **复验模板延续**：alpha.6 及后续 alpha 以同法抽查（源码锚点 + 发布产物探针 + 探针基线对账）。

## §6 风险与观察点（均非阻塞）

1. **alpha.6+ 发布后的增量抽查**：发布线当前仍为 alpha.5（本稿 §4-③ 复测确认）；后续新 alpha 发布后按 §5-5 模板抽查（源码锚若落后于发布锚，优先对发布产物探针）。
2. **`$DSH_HOME/cordis.patch.yml` 家庭层**（profile-boot.ts L64-66 新增语义）：用户可经家庭层覆盖 web 行；横幅 URL 恒打 `127.0.0.1`（index.ts L161 硬编码）故 URL_RE 健壮，端口覆盖由横幅读取自然吸收 —— 观察，无需改动。
3. **`DSH_TELEMETRY_DISABLED`**：新增环境开关（profile-boot.ts L76/L96/L170）；可选加固项（扩展 spawn env 注入可静默会话遥测），是否采纳留待用户决策。
4. **webserver compression**：web profile 开启 gzip level 1（cordis.patch.yml L116-124）；authProxy 透传实体头已兼容，且发布产物探针证实 gzip 中间件在位 —— 观察。
5. **`SIGTERM` 语义**（profile-boot.ts L221-224，监督者正常停止 → exit 0）：Windows 端扩展经 taskkill 路径终止，无影响 —— 观察。
6. **可选配置族**（`--trusted-host`/`trustedHosts`/`maxRequestBodyBytes`）：仅非回环/大请求场景相关，扩展 loopback-only 不触及 —— 观察。
7. **先行稿 v1 未提交**：两稿（v1/v2）同属 0.1.17 复验载体，A1 链闭环提交时按用户评审结论定稿谱系（保留双稿 + 谱系链，或合并），避免索引账目与 git 状态长期不一致。

---

> 评审人：待定 | 状态：待终审 | 日期：2026-09-02
